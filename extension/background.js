// Service worker for Moodboard Saver (Manifest V3)
// The keyboard shortcut (Cmd+Shift+S / Ctrl+Shift+S) is handled automatically
// by the browser via the "_execute_action" command in manifest.json —
// no extra code needed here.
